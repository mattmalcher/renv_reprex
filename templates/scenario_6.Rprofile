# Scenario 6: bioconductor.version workaround.
# renv::settings$bioconductor.version("3.20") is set in 10_init_project.R
# after renv::init(), so it is written to renv/settings.json.
# This file just activates renv normally.
if (file.exists("renv/activate.R")) source("renv/activate.R")
