# Scenario E: project-level Bioconductor version pinning.
# renv::settings$bioconductor.version("3.20") is called in 10_init_project.R
# after renv::init() so it can write to renv/settings.json.
# This file just activates renv normally.
if (file.exists("renv/activate.R")) source("renv/activate.R")
